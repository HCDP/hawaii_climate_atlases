events {
}

http {
    upstream rainfall-climate-atlas {
        server rainfall-climate-atlas:3000;
    }

    server {
        # Redirect HTTP requests to HTTPS.
        listen 80;
        server_name localhost;
        
        location / {
          # Send all traffic to the upstream defined above
          proxy_pass http://rainfall-climate-atlas;

          # Standard proxy headers
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # server {
    #     listen 443 ssl;

    #     server_name localhost;
    #     root /srv/public;
    #     server_tokens off;

    #     ssl_certificate /etc/nginx/ssl/my_ssl_cert.crt;
    #     ssl_certificate_key /etc/nginx/ssl/my_ssl_key.key;

    #     location / {
    #         try_files $uri $uri/ @nextjs;
    #     }

    #     location @nextjs {
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto https;
    #         proxy_set_header X-Forwarded-Ssl on;
    #         proxy_set_header Host $http_host;
    #         proxy_redirect off;
    #         proxy_pass http://nextjs;
    #         proxy_cookie_path / "/; HTTPOnly; Secure";
    #     }
    # }
}